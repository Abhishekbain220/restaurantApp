<%- include("./partials/header") %>

<div class="min-h-screen flex items-center justify-center bg-gray-100 p-8">
    <div class="bg-white p-8 rounded-lg shadow-lg max-w-lg w-full text-center">
        <h1 class="text-3xl font-bold text-gray-800 mb-6">Select Food Item</h1>
        
        <h2 class="text-2xl font-semibold text-gray-700 mb-4"><%= food.foodName %></h2>
        
        <img src="<%= food.foodImage %>" alt="Image of <%= food.foodName %>" 
             class="w-full h-64 object-cover rounded-lg mb-4 shadow-md">

        <h3 class="text-xl font-medium text-gray-600">₹<%= food.foodPrice %></h3>

        <div class="mt-6 flex justify-center space-x-4">
            <a href="/profile/deleteFood/<%= food._id %>" 
               class="bg-red-500 text-white px-4 py-2 rounded-lg font-semibold hover:bg-red-600 transition duration-300">
                Delete
            </a>
            <a href="/profile/updateFood/<%= food._id %>" 
               class="bg-blue-500 text-white px-4 py-2 rounded-lg font-semibold hover:bg-blue-600 transition duration-300">
                Update
            </a>
        </div>
    </div>
</div>

<%- include("./partials/footer") %>
